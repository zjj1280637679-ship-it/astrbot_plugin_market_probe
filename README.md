# 插件市场发布探针

这是一个严格最小、无业务功能的 AstrBot 插件，用于区分以下两类发布故障：

1. AstrBot Cloud 无法列出 GitHub App 可访问的仓库；
2. AstrBot Cloud 已读取仓库，但无法解析插件元数据。

插件本身不注册命令、工具、事件处理器或后台任务。它只用于发布链路诊断，
不会提交为正式插件市场条目。

## 文件集合

- `metadata.yaml`：符合官方发布文档的最小元数据；
- `main.py`：可被 AstrBot 加载、但不产生行为的插件入口；
- `LICENSE`：MIT 许可证。
